apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: test

labels:
  - pairs:
      app.kubernetes.io/managed-by: kustomize

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - deployment.yaml
  - deployment-job.yaml
  - service.yaml
  - networkpolicy.yaml
  - networkpolicy-job.yaml
